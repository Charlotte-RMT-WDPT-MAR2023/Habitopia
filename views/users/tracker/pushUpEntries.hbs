
<img class = "background" src="./images/background.jpg" alt="habits">

<a href="/pushups" class="return-link">Return</a>
<br>


<h1>Push-Ups: Previous Entries</h1>

{{#each entriesByDate}}
  <h2>{{@key}}</h2> 
  
  {{#each this}}
    <div>
      <p> Number of Push-Ups: {{content}}</p> 
    </div>

   {{!-- <form action="/entries/pushups/edit/{{_id}}" method="POST">
    <button type="submit" class="edit-side-by-side">Edit</button>
  </form> --}}

    <form action="/entries/pushups/delete/{{entryId}}" method="POST">
      <button class="habit-button" type="submit" onclick="return confirm('Are you sure you want to delete this item?')" class="delete-side-by-side">Delete</button>
    </form>

    </div>
  {{/each}}
{{/each}}



